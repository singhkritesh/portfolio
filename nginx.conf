server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /portfolio {
        rewrite ^/portfolio(/.*)$ $1 last;
        rewrite ^/portfolio$ / last;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
